version: '3'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./src:/usr/share/nginx/html
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
